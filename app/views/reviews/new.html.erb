<div class="container mt-3">
<h2>New review</h2>
  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :rating %>
    <%= f.input :content, data: { editor: 'markdown' }, rows: 10 %>
    <%= f.submit "Submit review", class: 'btn btn-primary my-4' %>
  <% end %>
</div>
